The application of closed orbit-based optics correction LOCO for FCC-ee lattices is demonstrate.
The code was implemented using the Python accelerator toolbox (PyAT). The impact of alignment errors on FCC the
lattice optics parameters were studied
